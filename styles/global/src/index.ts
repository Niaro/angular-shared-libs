// we need the const to be able to build the package this way other libs can dependent on it
// but we have only styles here thus we have a dummy const

export const dummy = 'should export something to build the package';
